'use babel';

import BgTreeViewToolbarView from '../lib/bg-tree-view-toolbar-view';

describe('BgTreeViewToolbarView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
